# Production: full URL to the FastAPI v1 root (no trailing slash after v1).
# Example: https://rbac-api-example.onrender.com/api/v1
# The SPA calls paths like /auth/login relative to this base.
# CORS on the API must allow your Vercel origin (e.g. https://your-app.vercel.app).
VITE_API_URL=
